import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "giant-flies-matter.loca.lt",
    "*.loca.lt",
    "a3825ace913061.lhr.life",
    "*.lhr.life",
  ],
};

export default nextConfig;
